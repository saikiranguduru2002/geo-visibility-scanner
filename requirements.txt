# GEO scanner API + scanner module
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pydantic>=2.0.0
python-dotenv>=1.0.0
# Optional: Wikidata SPARQL (scanner uses requests by default; SPARQLWrapper available if you extend queries)
SPARQLWrapper>=2.0.0
